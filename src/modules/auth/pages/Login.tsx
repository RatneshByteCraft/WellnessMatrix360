import { Link, useNavigate } from 'react-router-dom'
export default function Login(){const nav=useNavigate();return <div className="auth-card"><h2>Sign in</h2><p>Use your organization credentials.</p><label>Email or mobile<input defaultValue="manager@veda.example"/></label><label>Password<input type="password" defaultValue="password"/></label><div className="auth-row"><label className="check"><input type="checkbox"/> Remember me</label><Link to="/forgot-password">Forgot password?</Link></div><button className="btn primary full" onClick={()=>nav('/dashboard')}>Sign in securely</button><small className="muted">MFA challenge is applied when required by tenant policy.</small></div>}
